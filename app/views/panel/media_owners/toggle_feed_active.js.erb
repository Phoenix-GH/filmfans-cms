if("<%=@media_owner.feed_active %>" == "true"){
  $("#toggle_<%=@media_owner.id%>").find('a').addClass('btn-status-active').find('.status').text('Active');
} else {
  $("#toggle_<%=@media_owner.id%>").find('a').removeClass('btn-status-active').find('.status').text('Inactive');
}
