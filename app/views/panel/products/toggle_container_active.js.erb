if("<%=@product.containers_placement%>" == "true"){
  $("#toggle_<%=@product.id%>").find('a').addClass('btn-status-active').find('.status').text('Active');
} else {
  $("#toggle_<%=@product.id%>").find('a').removeClass('btn-status-active').find('.status').text('Inactive');
}
